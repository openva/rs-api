version: '3'
services:
  api:
    build:
      context: deploy/
      dockerfile: ./API.Dockerfile
    links:
      - db:db
    ports:
      - "5001:80"
    volumes:
      - ./htdocs:/var/www/html/
  db:
    build:
      context: deploy/
      dockerfile: ./MySQL.Dockerfile
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    ports:
      - "3306:3306"
volumes:
    db_data: {}
